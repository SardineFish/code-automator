# Design Docs

- `core-beliefs.md`: the repo-level beliefs that should shape the webhook automation service.
- `workflow-config.md`: the YAML config contract, interpolation rules, and workflow matching model.
